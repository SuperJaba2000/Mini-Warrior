const gameVersion = "PC-0.01"
const screenSize = { height: 15, width: 31 };

const Settings = {
	graphics: {
		useTextures: false,
	},

    debug: {
		tileSize: (32),
		pause: false,
	        scl: 0.07,
	},
}, n = null;