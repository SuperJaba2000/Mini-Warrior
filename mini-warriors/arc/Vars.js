const gameVersion = "PC-0.04-DEBUG"
const screenSize = { height: 0, width:  0};

const Settings = {
	graphics: {
		useTextures: false,
	},

    debug: {
		tileSize: (32),
		pause: false,
	},
}, n = null;
