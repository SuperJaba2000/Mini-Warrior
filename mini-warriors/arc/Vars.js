const gameVersion = "PC-0.03-DEBUG"
const screenSize = { height: 0, width:  0};

const Settings = {
	graphics: {
		useTextures: true,
	},

    debug: {
		tileSize: (32),
		pause: false,
	},
}, n = null;