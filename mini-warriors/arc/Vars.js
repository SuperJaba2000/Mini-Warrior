const gameVersion = "PC-0.03-DEBUG"
const screenSize = { height: 0, width:  0};

const Settings = {
	graphics: {
		useTextures: false,
	},

    debug: {
		tileSize: (16),
		pause: false,
	},
}, n = null;